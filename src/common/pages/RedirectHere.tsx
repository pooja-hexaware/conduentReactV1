import { Typography } from "@mui/material";

const RedirectHere = () => {
  return (
    <div className="snippet flex flex-col items-center justify-center h-full">
      <Typography variant="h5">Redirecting</Typography>
    </div>
  );
};

export default RedirectHere;
